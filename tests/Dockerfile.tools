# artsore-tools — утилитарный образ для Init Job и тестовых скриптов.
# Содержит bash, curl, jq — всё, что нужно для работы с API.
FROM alpine:3.19

RUN apk add --no-cache bash curl jq

ENTRYPOINT ["/bin/bash"]
