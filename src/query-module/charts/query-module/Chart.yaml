apiVersion: v2
name: query-module
description: >-
  Helm chart для Query Module — модуль поиска и скачивания файлов Artstore.
  Поиск по атрибутам (PostgreSQL), LRU cache, proxy download через SE,
  lazy cleanup. Stateless Deployment + ClusterIP Service + HTTPRoute.
type: application
version: 0.1.0
appVersion: "0.1.0"
keywords:
  - artstore
  - query-module
  - search
  - download
maintainers:
  - name: Artur Kryukov
